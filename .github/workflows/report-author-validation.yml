name: Report author display validation

on:
  pull_request:
    paths:
      - 'src/main/java/com/example/toiletapi/report/**'
      - 'src/main/java/com/example/toiletapi/auth/**'
      - 'src/test/java/com/example/toiletapi/report/**'
      - '.github/workflows/report-author-validation.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - name: Verify author privacy and report regression
        run: |
          chmod +x ./gradlew
          ./gradlew test --tests '*ReportAuthorDisplayTest' --tests '*ToiletReportServiceTest' --tests '*WithdrawalSecurityTest' --no-daemon
